{
  "name": "mvp-ui",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "node scripts/check-dark-safe.mjs && biome lint .",
    "lint:dark": "node scripts/check-dark-safe.mjs",
    "format": "biome format --write .",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@changesets/cli": "latest"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
